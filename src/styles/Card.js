import styled from "styled-components";

const Card = styled.div`
  padding: 1.6rem;
  border-radius: 21.33px;
  background-color: #ebeaea;
  box-shadow: 8px 8px 18px 0px rgba(0, 0, 0, 0.1),
    -8px -8px 18px 0px rgba(255, 255, 255, 0.8);
`;

export default Card;
